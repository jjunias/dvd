<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="basket">
	<insert id="insert" parameterType="basketDto">
		INSERT INTO dvd_basket
		VALUES(#{id}, #{num})
	</insert>
	
	<delete id="delete" parameterType="basketDto">
		DELETE FROM dvd_basket
		WHERE id=#{id}
		AND num=#{num}
	</delete>
	
	<select id="getList" parameterType="string" resultType="basketDto">
		SELECT bd.id, dv.title, dv.price
		FROM (SELECT * FROM dvd_basket WHERE id = #{id}) bs, dvd dv
		WHERE dv.num = bs.num
	</select>
</mapper>